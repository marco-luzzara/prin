services:
# ----------------------------- FILE WATCHER -----------------------------
  data-loading-webapp:
    profiles:
      - without-visualization
      - complete
      - data-loading-testing

# ----------------------------- KAFKA -----------------------------
  kafka-broker-0:
    profiles:
      - ranger-test
      - without-visualization
      - complete
      - data-loading-testing

  # kafka-broker-1:
  #   profiles:
  #     - ranger-test

  kafka-init:
    profiles:
      - ranger-test
      - without-visualization
      - complete
      - data-loading-testing

# ----------------------------- HIVE METASTORE -----------------------------
  hive-metastore:
    profiles:
      - complete
      - ranger-test
      - without-visualization

# ----------------------------- MINIO -----------------------------
  minio:
    profiles:
      - complete
      - ranger-test
      - without-visualization

  minio-init:
    profiles:
      - complete
      - ranger-test
      - without-visualization

# ----------------------------- TRINO -----------------------------
  trino:
    profiles:
      - ranger-test
      - sql-ui
      - without-visualization
      - complete

# ----------------------------- CDC -----------------------------
  cdc-script:
    profiles:
      - complete
      - without-visualization

# ----------------------------- HUE -----------------------------
  hue:
    profiles:
      - sql-ui
      - complete

# ----------------------------- SUPERSET -----------------------------
  superset:
    profiles:
      - sql-ui
      - complete

# ----------------------------- RANGER -----------------------------
  ranger:
    profiles:
      - ranger-test
      - sql-ui
      - without-visualization
      - complete

  ranger-zk:
    profiles:
      - ranger-test
      - sql-ui
      - without-visualization
      - complete

  ranger-solr:
    profiles:
      - ranger-test
      - sql-ui
      - without-visualization
      - complete

  ranger-db:
    profiles:
      - ranger-test
      - sql-ui
      - without-visualization
      - complete