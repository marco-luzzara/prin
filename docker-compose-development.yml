services:
  data-loading-webapp:
    develop:
      watch:
        - action: sync+restart
          path: ./remote_machine/webapp
          target: /app/webapp
        - action: rebuild
          path: ./remote_machine/requirements.txt